# Принимаемые параметры:

model_parameters = {
    'n_features': 30,
    'hidden_dim': 32
}

train_parameters = {
    'epochs': 15,
    'batch_size': 16,
    'lr': 0.0001
}

# User-defined параметры

time_size, shuffle
